import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  titleScreen: {
    fontWeight: 'bold',
    fontSize: 25,
    color: '#fff',
    marginVertical: 10,
  },
});

export default styles;
